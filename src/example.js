const username = "freddy";
typeof username === "string";

const newLocal = "serviceWorkder";
if (!(newLocal in navigator)) {
  console.log(navigator);
}

const greeting = "hello";

console.log(`${greeting} world`);

[1, 2, 3].forEach((x) => console.log(x));
